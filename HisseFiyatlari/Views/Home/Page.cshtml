@model HisseFiyatlari.Models.Menu
@{
    ViewData["Title"] = Model.Title;
}

<div class="container mt-4 mb-5 bg-white p-4 shadow-sm rounded">
    <h2 class="border-bottom pb-2 mb-4 text-primary">@Model.Title</h2>

    <div class="page-content">
        @Html.Raw(Model.Content)
    </div>
</div>